/*
let biggest = 0;
for (i= lines[0]; i <100; i++){
    if(lines[i] !== biggest){
        biggest == lines[i]
    }
}

for (i=lines[0]; i<100; i++){
    console.log(Math.max(0, lines.values));
}

function getMaxOfArray(numArray) {
    return Math.max.apply(null, numArray);
  }
var array = [3, 4, 5, 21.15, 21, 9];
var largest = 0;

    for (i=0; i<array.length; i++) {
    if (array[i]>largest) {
        largest = array[i];
    }
}
console.log(largest);
/*var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');
const largestNumber = (values) => {
        let highest = 0;
        for (let i=0; i<values.length; i++){
            if(values[i] > highest){
                highest = values[i];
            }
        }
        return highest;
    };
    console.log(largestNumber(lines));*/*/